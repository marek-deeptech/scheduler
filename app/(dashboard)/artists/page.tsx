'use client'

import TeamSection from '@/components/TeamSection'

export default function ArtistsPage() {
  return (
    <TeamSection
      teamName="Cast"
      title="Artyści"
      emptyIcon="🎭"
      sectionLabel="Artyści"
      removeLabel="Usunąć tę osobę?"
    />
  )
}
